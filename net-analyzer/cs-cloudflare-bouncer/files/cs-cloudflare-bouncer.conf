# OpenRC service providing the CrowdSec LAPI used by the Cloudflare bouncer.
# The init script defaults to the local crowdsec service if this setting or
# this file is absent.
lapi_init_service="crowdsec"

# For a remote LAPI, disable the local service dependency and optionally order
# startup after networking and DNS.
#lapi_init_service="none"
#rc_after="net dns"
